# Paper workspace

This directory contains manuscript-facing assets only:

- `figures/`: final compact figures promoted from evaluated runs;
- `tables/`: final CSV/Markdown/LaTeX tables;
- the manuscript will be added after the dataset decision gate.

Raw logs, checkpoints, videos, and per-seed outputs belong in ignored
`outputs/`, not here.

